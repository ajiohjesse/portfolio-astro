---
// You might want to add form handling logic here later
---

<section class="container py-24 bg-gray-50">
  <h2 class="text-4xl font-bold text-center mb-12">Let's Connect</h2>
  <div class="max-w-xl mx-auto">
    <form class="bg-white p-8 rounded-lg shadow-md">
      <div class="grid md:grid-cols-2 gap-4 mb-4">
        <input
          type="text"
          placeholder="Name"
          class="w-full p-3 border border-gray-300 rounded focus:border-emerald-500 transition"
        />
        <input
          type="email"
          placeholder="Email"
          class="w-full p-3 border border-gray-300 rounded focus:border-emerald-500 transition"
        />
      </div>
      <textarea
        placeholder="Your Message"
        rows="4"
        class="w-full p-3 border border-gray-300 rounded mb-4 focus:border-emerald-500 transition"
      ></textarea>
      <button
        type="submit"
        class="w-full bg-emerald-600 text-white py-3 rounded hover:bg-emerald-700 transition"
      >
        Send Message
      </button>
    </form>
    <div class="mt-8 text-center">
      <p class="text-gray-600 mb-4">Or find me on:</p>
      <div class="flex justify-center space-x-4">
        <a href="#" class="text-gray-600 hover:text-emerald-600">LinkedIn</a>
        <a href="#" class="text-gray-600 hover:text-emerald-600">GitHub</a>
        <a href="#" class="text-gray-600 hover:text-emerald-600">Twitter</a>
      </div>
    </div>
  </div>
</section>
